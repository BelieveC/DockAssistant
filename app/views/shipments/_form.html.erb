<%= form_for(shipment) do |f| %>
  <% if shipment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shipment.errors.count, "error") %> prohibited this shipment from being saved:</h2>

      <ul>
      <% shipment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dock_worker %>
    <%= f.collection_select :dock_worker_id, DockWorker.all,:id,:last_name %>
  </div>

  <div class="field">
    <%= f.label :appointment %>
    <%= f.collection_select :appointment_id, Appointment.all,:id, :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
