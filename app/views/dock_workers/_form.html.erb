<%= form_for(dock_worker) do |f| %>
  <% if dock_worker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dock_worker.errors.count, "error") %> prohibited this dock_worker from being saved:</h2>

      <ul>
      <% dock_worker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :last_name %>
    <%= f.text_field :last_name %>
  </div>

  <div class="field">
    <%= f.label :first_name %>
    <%= f.text_field :first_name %>
  </div>

  <div class="field">
    <%= f.label :address %>
    <%= f.text_field :address %>
  </div>

  <div class="field">
    <%= f.label :state_id %>
    <%= f.collection_select :state_id, State.all, :id, :name, { :prompt => 'Select a State', :selected => @dock_worker.state_id } %>
  </div>

  <div class="field">
    <%= f.label :zipcode %>
    <%= f.text_field :zipcode %>
  </div>

  <div class="field">
    <%= f.label :pay_rate %>
    <%= f.text_field :pay_rate %>
  </div>

  <div class="field">
    <%= f.hidden_field :archived %>
    <%= f.hidden_field :archived, :value => false %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
